---
releases:

- name: Jakarta Persistence 3.0
  version: 3
  schemas:

    - schema: orm_3_0.xsd
      namespace: persistence/orm
      description: XML Schema for for the persistence object/relational mapping file
      status: Final

    - schema: persistence_3_0.xsd
      namespace: persistence
      description: XML Schema for the persistence configuration file
      status: Final

- name: Jakarta Persistence 3.1
  version: 3.1
  schemas:

    - schema: orm_3_1.xsd
      namespace: persistence/orm
      description: XML Schema for for the persistence object/relational mapping file
      status: Final

- name: Jakarta Persistence 3.2
  version: 3.2
  schemas:

    - schema: orm_3_2.xsd
      namespace: persistence/orm
      description: XML Schema for for the persistence object/relational mapping file
      status: Final

    - schema: persistence_3_2.xsd
      namespace: persistence
      description: XML Schema for the persistence configuration file
      status: Final

# Template: Example for creating new schema entry
#   - schema: a_simple_example.xsd
#     description: Some text descring the schema
#     status: Final Release | Draft

# Template: Example for creating a new Jakarta Persistencde release
# - name: Jakarta Persistence 4.0
#   version: 4
#   schemas:
